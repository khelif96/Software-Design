Class Object
    - Exists in package java.lang
    - Object has no fields so you don't need to initialize so has no constructor/ empty constructor
    - Immediatley returns control to the super class constructor

    Methods
        public methods
        - .toString();
        - .equals(); compares two objects for equality
        protected methods
        - .clone(); makes a copy of an object and returns a reference to it


Constructors
    - In java classes cannot inherit constructors
    - The constructor of a subclass must implicitly or explicitly call the constructor of the superclass
        to initialize the instance variables inherited from the super class'
    - The last constructor to be executed is the first one we start with

    see Images/image2.jpg

    - Implicit calls the default or no argument constructor
<code>
    // Explicit call to the constructor
    public subClass(){
        super.superClass();
    }
<code>

    public SubClassName extends SuperClassName{

    }
    public SuperClassName extends Object{ // Class object exists in package java.lang

    }

</code>